---
schema_version: 1
id: "iss-9"
slug: "release-workflow-ships-any-v-tag-as-a-full-release-rc-tags"
severity: "minor"
category: "bug"
source: "agent-finding"
found_during: "2026-07 bug-hunt round 5"
found_at: ".github/workflows/release.yml"
resolution: "release.yml's publish step now classifies the tag by its own shape and takes one of two paths. A pre-release-shaped tag (a hyphen, which is what introduces the pre-release identifiers under semantic versioning) is published with --prerelease and --latest=false, and no election runs for it. Any other tag elects as before, with two corrections. There were two independent defects and either alone left the hole open. First, the create call carried no --prerelease anywhere in the file, so a candidate became an ordinary release that no later --exclude-pre-releases query could filter. Second — surfaced by a peer session rather than by me — the candidate list was fetched with that exclusion and then had the tag under consideration appended to it before the sort, so the exclusion protected the comparison and not the outcome; even a correctly flagged candidate won the election against itself. Not electing at all on a pre-release tag closes the second by construction. The election is additionally seeded from the tag and displaced only by a released tag that beats it, with pre-release-shaped candidates skipped, so a candidate published as a full release before this rule existed cannot pin latest for ever. Measured rather than reasoned: without the filter, v0.3.1, v0.4.0-rc1 and v0.4.0 through sort -V yields v0.4.0-rc1, which is the bug; with it the answer is v0.4.0. Two things were found by running the code rather than reading it. The obvious filter spelling, grep -v with a bare hyphen pattern, is refused outright by at least one grep implementation people install over the system one, so the filter is the shell's own case statement and depends on no external binary. And accumulating a newline-separated list inside an indented block folds that indentation into the values, so the comparison is pairwise instead. Held by two tests in internal/sitetest, watched to fail against the pre-change workflow and pass after; full suite green under -race, gofmt and vet clean. The stale body claiming the defect is latent because only v0.1.0 and v0.1.1 were ever pushed no longer holds — 0.3.1 has shipped — and the installer resolves the latest-release endpoint, so the next candidate tag would have installed itself to every new user."
impact: fix
---

The release workflow publishes any `v*` tag as a full release, so a prerelease-style tag (e.g. `v0.4.0-rc1`) can capture `releases/latest` and ship to every installer. The trigger matches all `v*` tags (release.yml:26-27), `gh release create` never passes `--prerelease` (release.yml:183-185), and the latest election compares tags with `sort -V` (release.yml:178-181) — which orders `v0.4.0-rc1` *after* `v0.4.0` (verified empirically on GNU coreutils 9.4; `sort -V` is not semver, where a pre-release precedes its release). Two consequences compound:

- Pushing `v0.4.0-rc1` while `v0.3.0` is current creates a normal (non-prerelease) release that wins the election and gets `--latest=true`; every `install.sh` run (`releases/latest/download`, install.sh:69) immediately ships the RC, validly signed.
- Because the RC was created as a *full* release, the election's `--exclude-pre-releases` never excludes it afterwards, so when `v0.4.0` final ships, `sort -V` still ranks `v0.4.0-rc1` newest and the final is published with `--latest=false` — `latest` stays pinned to the RC.

Latent today: the only tags ever pushed are `v0.1.0` and `v0.1.1`, the workflow's documented contract is plain `vX.Y.Z` tags, and no RC practice exists in this repo. Distinct from the round-2 fix, which addressed old-tag re-releases via the dispatch path.

Deferred rather than auto-patched because the fix encodes an unmade versioning policy on signed-release infrastructure: treating hyphenated tags as prereleases (`--prerelease` + excluding them from the election) adopts semver-ish semantics the repo never declared, while tightening the trigger pattern to `vX.Y.Z` forecloses RC tagging entirely. Either is a one-liner once the maintainer picks the policy; until then, avoid pushing hyphenated `v*` tags.

## Grounds

- pursued: we expect classifying by tag shape to be safe because it needs no state and no lookup — the tag alone carries the fact under semantic versioning, so nothing can fall out of step with the forge the way a flag set at creation time can. We are wrong if this project ever wants a tag carrying a hyphen for a reason other than a pre-release, for example a build or platform suffix, at which point such a tag would be silently published as a pre-release and never become latest; that would show as a real release the installer refuses to fetch, and the remedy is to narrow the pattern to the known candidate spellings rather than to any hyphen. Pursued now rather than with the updater because the defect is already live: the installer resolves the latest-release endpoint today, so the exposure does not wait for an in-product updater to exist.
