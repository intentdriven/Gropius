# Decisions — one dated line each; architecture-shaping entries graduate to ADRs

- 2026-07-29: Adopted the three-tier `.abcd/` working-state layout; the root `DECISIONS.md` (verified spike facts) moved to `.abcd/development/decisions/DECISIONS.md`; handover notes moved to the gitignored local tier.
- 2026-07-29: Deferred findings from the 2026-07 audit rounds captured as ledger issues under `.abcd/work/issues/` so they survive a clone; the handover file is no longer their home.
- 2026-07-29: History rewritten (git filter-repo, maintainer-pushed) to purge a committed handover file containing machine-local network details; handover notes live only in the gitignored local tier from now on.
- 2026-07-29: Automated bug-hunt round 1: fixed the hub 416 retry loop, non-loopback ExposedToLAN classification, nested-file rescan, restart-required flag, body-read status codes, browser-open zombies, `make clean` deleting the committed icon, and version stamping, plus doc corrections; deferred the cross-account singleton contradiction (iss-5) and eviction drain accounting (iss-6) to the ledger.
- 2026-07-29: Automated bug-hunt round 2: kept a failed re-download from demoting a ready model, stopped loopback-only binds advertising the `.local` endpoint, made installs quit a running app first, kept old-tag re-releases from hijacking "latest", isolated the signing key from `brew install`, added the release gofmt gate, gitignored `.abcd/.work.local/`, accepted Apple Silicon under Rosetta, plus doc and comment-spelling corrections; deferred the specific-IP bind control-panel lockout (iss-7) to the ledger.
- 2026-07-30: Automated bug-hunt round 3: reap a model server that exits after becoming ready (previously a permanent-502 corpse charging the memory budget), recheck that a running app actually quit in `make install`, stamp the client app's bundle version from the release tag, document the client's macOS 26 SDK build requirement, and correct the hub transport's stalled-reads comment; recorded on iss-2 that its fix must also cover the Acquire/launch path.
- 2026-07-30: Automated bug-hunt round 4: fixed a hub download progress undercount (a wrong-sized final file beside a resumable `.part` left the resumed prefix uncredited, so a successful download stopped short of 100%; `existingBytes` now sums both and the stray-`.part` removal uncounts it) and respelled three British code comments US; deferred two confirmed substantive findings — the instance-token replay/impersonation vector via `GET /api/instance` (folded into iss-5, whose "0600 keeps the token secret" premise it corrects) and the reaper's exact-equality `kern.boottime` gate that macOS clock steps silently defeat (new iss-8) — both trust-boundary design decisions.
